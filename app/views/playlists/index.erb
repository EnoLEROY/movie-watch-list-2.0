
<div class="d-flex top-bar">
  <h2>Playlists</h2>
</div>

<div class="container">
  <div class="card">
    <% @playlists.each do |playlist| %>
      <%= playlist.name %>
    <% end %>
  </div>
</div>
