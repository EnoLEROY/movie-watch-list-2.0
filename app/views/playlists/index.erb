
<div class="top-bar">
  <h2>Playlists</h2>
</div>

<div class="container">
  <% @playlists.each do |playlist| %>
    <%= link_to playlist_path(playlist) do %>
    <div class="card">
      <%= playlist.name %>
    </div>
    <% end %>
  <% end %>
</div>
